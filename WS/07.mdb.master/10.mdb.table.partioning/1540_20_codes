Before trying the code:

\W
CREATE TABLE article (
	id INTEGER UNSIGNED NOT NULL AUTO_INCREMENT,
	date DATE NOT NULL,
	author VARCHAR(100),
	language TINYINT UNSIGNED,
	text TEXT,
	PRIMARY KEY (id, date)
)
	ENGINE = InnoDB
PARTITION BY RANGE (YEAR(date)) (
	PARTITION p0 VALUES LESS THAN (1990),
	PARTITION p1 VALUES LESS THAN (2000),
	PARTITION p3 VALUES LESS THAN (2010),
	PARTITION current VALUES LESS THAN (2020)
);

SQL:

ANALYZE TABLE article;
ALTER TABLE article ANALYZE PARTITION p0, p1, p2;
ALTER TABLE article ANALYZE PARTITION ALL;
ALTER TABLE article ANALYZE PARTITION p0;
